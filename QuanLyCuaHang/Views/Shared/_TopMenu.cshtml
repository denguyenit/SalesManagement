<nav class="navbar navbar-expand-lg navbar-light bg-danger">
    <div class="container">
        <a class="navbar-brand" href="@Url.Action("Index", "Home")">
            <img src="~/Image/logo.png" alt="" width="50" height="50" class="d-inline-block align-text-center">
            <b style="font-size:x-large; color:white">D5-STORE</b>
        </a>
        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <form class="d-flex" action="@Url.Action("Timkiem","Home")" method="post">
                <input class="form-control me-1" style="width:400px" type="search" placeholder="Tìm kiếm sản phẩm..." aria-label="Search" name="searchString">
                <button class="btn btn-dark" type="submit"><i class="fas fa-search"></i></button>
            </form>
        </div>
        <a href="@Url.Action("Giohangcuaban","Giohang")" class="btn btn-outline-light" type="submit" style="margin:0px 10px 0px 0px">
            <i class="fas fa-cart-plus"></i>
            Giỏ hàng
            <span class="badge bg-dark text-white ms-1 rounded-pill">@ViewBag.TongGH</span>
        </a>
        @if (Session["TenKH"] != null)
        {           
            <div class="dropdown">
                <a class="btn btn-outline-light dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="fas fa-user"></i>
                    <b style="font-size:20px; color:white">@Session["TenKH"]</b>
                </a>
                <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                    <li><a class="dropdown-item" href="@Url.Action("Dangxuat", "KH")">Đăng xuất</a></li>                    
                </ul>
            </div>
        }
        else
        {
            <a class="navbar-brand" href="@Url.Action("Dangnhap", "KH")">
                <i class="fas fa-user"></i>
                <b style="font-size:20px; color:white">Đăng nhập</b>
            </a>
            <a class="navbar-brand" href="@Url.Action("Dangky", "KH")">
                <i class="fas fa-user"></i>
                <b style="font-size:20px; color:white">Đăng ký</b>
            </a>
        }
    </div>
</nav>
<nav class="navbar navbar-expand-lg navbar-expand bg-dark">
    <div class="container">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    @Html.ActionLink("TRANG CHỦ", "Index", "Home", new { area = "" }, new { @class = "nav-link" })
                </li>

                <li class="nav-item">
                    @Html.ActionLink("GIỚI THIỆU", "Gioithieu", "Home", new { area = "" }, new { @class = "nav-link" })
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        SẢN PHẨM
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @foreach (var item in ViewBag.MenuSP)
                        {
                            <li><a class="dropdown-item" href="@Url.Action("Danhmucsanpham", "Sanpham", new { id=item.MaDMSP })">@item.TenDMSP</a></li>
                        }
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        TIN TỨC
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @foreach (var item in ViewBag.MenuTT)
                        {
                            <li><a class="dropdown-item" href="@Url.Action("Tintuc", "Tintuc", new { id=item.MaDMBV })">@item.TenDMBV</a></li>
                        }
                    </ul>
                </li>
                <li class="nav-item">
                    @Html.ActionLink("LIÊN HỆ", "Lienhe", "Home", new { area = "" }, new { @class = "nav-link" })
                </li>
            </ul>
        </div>
        <div class="col-6">
            <marquee behavior="scroll" direction="left" style="color:white;font-size:medium">⚡⚡⚡Flash sale⚡⚡⚡Cùng D5-STORE săn iphone 13⚡⚡⚡</marquee>
        </div>
    </div>
</nav>

